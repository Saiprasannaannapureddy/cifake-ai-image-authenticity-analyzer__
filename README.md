# Dataset

The CIFAKE dataset is not included in this repository because of its size.

Download/use the dataset according to its license and place it in this structure:

```text
dataset/
├── train/
│   ├── FAKE/
│   └── REAL/
└── test/
    ├── FAKE/
    └── REAL/
```
